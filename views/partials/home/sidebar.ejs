<link href="/vendor/clock/clock.css " rel="stylesheet " type="text/css ">
<script src="/vendor/clock/clock.js "></script>

<style>
    .categorylist {
        cursor: pointer;
    }
    .categorylist :hover{
        text-underline-position: below;
        text-decoration: underline;
    }
</style>
<!-- Search Widget -->
<div style="display: block">
    <div class="card">
        <h5 class="card-header">Categories</h5>
        <div class="card-body">
            <div class="row justify-content-center">
                <div class="col-12">
                    <ul class="list-group-item mb-0 text-center text-capitalize categorylist">
                        <%if(gcate == null) {%>
                        <li class="list-group-item categorylist"> No Category <br>
                            <a href="/category/add">
                                Add Category
                            </a>
                        </li>
                        <%}else{%>
                        <%gcate.forEach(cate => {%>
                        <li class="list-group">
                            <a class="categorylist-item" onclick="test(<%=cate.id%>,'<%=cate.name%>')">
                                <%=cate.name%>
                            </a>
                        </li>
                        <%})%> <%}%>
                </ul>               
            </div>
        </div>
        </div>
    </div>
    --
    <!-- Calendar Widget -->
    <div class="card my-auto">
        <h5 class="card-header">Calendar</h5>
        <div id="clockdate" class="card-body">
            <div class="clockdate-wrapper">
                <div id="clock"></div>
                <div id="clocksec"></div>
                <div id="date"></div>
            </div>
        </div>
    </div>
</div>

<script>
    function test(data, name) {
        console.log(window.location.pathname);
        if (window.location.pathname != '/') {
            window.location.pathname = '/';
            categoryAjax(data, name);
        }
        if (window.location.pathname == '/') {
            categoryAjax(data, name);
        }
    }

    function categoryAjax(data, name) {
        console.log((data));
        console.log('it get click');
        $.ajax({
            type: `GET`,
            url: `/product/Category/${data}?name=${name}&sortby=none`,
            success: result => {
                if (result.error) {
                    $('#productPart').html(result.htmlData);
                } else {
                    $(`#productPart`).html(result.htmlData);
                }
            }
        });
    }
</script>